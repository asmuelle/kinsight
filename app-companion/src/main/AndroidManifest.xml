<?xml version="1.0" encoding="utf-8"?>
<!--
  Invariant 1: this manifest must NEVER declare android.permission.INTERNET.
  Alert delivery arrives through core/transport's relay client (M2);
  RepoInvariantsTest enforces this in CI.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.KinSightCompanion">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
